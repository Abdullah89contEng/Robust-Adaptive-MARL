# Two-Phase Exchangeable-Context CCM-FMASAC -- Python dependencies
# Tested with Python 3.11 (conda env "marl"). Versions pinned to a known-good set.
#
# NOTE: torch here is the CPU build. To get that exact wheel:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
# then:  pip install -r requirements.txt

# --- core: RL / multi-agent / environment ---
torch==2.12.0
torchrl==0.11.1
tensordict==0.11.0
vmas==1.5.2
benchmarl==1.5.2

# --- numerics / data / plotting ---
numpy==1.26.4
pandas==3.0.3
matplotlib==3.11.0
scipy==1.17.1
PyYAML==6.0.3

# --- evaluation & diagnostics ---
scikit-learn==1.9.0

# --- notebooks (main.ipynb, training_report.ipynb, deployment_test.ipynb) ---
nbformat==5.10.4
nbconvert==7.17.1
ipython==9.14.1
